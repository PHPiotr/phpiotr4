const initialState = {
    collection: {},
    document: {},
    errorMessage: '',
    errors: {},
    inserted: {},
};

const hostels = (state = initialState, action) => {

};

export default hostels;