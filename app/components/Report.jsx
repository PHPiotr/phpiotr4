var React = require('react');
var ReportForm = require('./ReportForm.jsx');

module.exports = React.createClass({
    render: function() {
        return(
                <ReportForm />
        );
    }
});